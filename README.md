# Plain Text output

Extends the `troff` transform type to create `text`, plain text
output from DITA content. For a more human-readable output
of tables ans syntax diagrams, I would recommend installing
the `org.dita.text-asciiart plug-in.